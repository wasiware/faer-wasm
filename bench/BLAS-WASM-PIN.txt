# The BLAS layer lives in its own repo since the 2026-07-20 split.
# Market-race scripts (gemm-tune-ab.mjs, cplx-gemm-ab.mjs) need its
# bench module (blas_bench.wasm); build it from the pinned revision —
# same philosophy as the faer pin in patches/UPSTREAM-BASE.txt:
# nothing moves under us without an explicit re-pin + gate re-run.
repo: https://github.com/wasiware/blas.wasm
rev:  579beba0e498f978510e0e5a2b962867d1102b0f
build:
  git clone https://github.com/wasiware/blas.wasm && cd blas.wasm
  git checkout <rev>
  cd bench && cargo build --release --target wasm32-unknown-unknown --lib
  # -> target/wasm32-unknown-unknown/release/blas_bench.wasm
